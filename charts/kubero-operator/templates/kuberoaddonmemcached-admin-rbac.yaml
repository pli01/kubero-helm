apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "kubero-operator.fullname" . }}-kuberoaddonmemcached-admin-role
  labels:
  {{- include "kubero-operator.labels" . | nindent 4 }}
rules:
- apiGroups:
  - application.kubero.dev
  resources:
  - kuberoaddonmemcacheds
  verbs:
  - '*'
- apiGroups:
  - application.kubero.dev
  resources:
  - kuberoaddonmemcacheds/status
  verbs:
  - get
